# 双臂机械臂（天链 TCB605 × 2）使用文档

## 一、硬件与网络

| 项目 | 左臂 | 右臂 |
|---|---|---|
| 型号 | TCB605 | TCB605 |
| 控制器 IP | 192.168.1.13 | 192.168.1.14 |
| 指令端口 | 6001 | 6001 |
| 实时数据端口 | 7000 | 7000 |

- 电脑网口需在 `192.168.1.x` 网段（与雷达共用，主机 IP 当前为 192.168.1.5）
- 启动前自检：`ping 192.168.1.13` 和 `ping 192.168.1.14` 都应通

## 二、启动

每个新终端先 source：

```bash
source /home/kybot/kybot_ros2_ws/install/setup.bash
```

### 双臂同时启动（推荐）

```bash
ros2 launch tl_bringup tl_tcb605_dual_bringup.launch.py
```

### 单臂启动（旧方式，仍可用）

```bash
ros2 launch tl_bringup tl_tcb605_bringup.launch.py   # 默认连 192.168.1.13
```

单臂模式下的服务名与旧版完全一致（如 `/tl_driver/connect_arm`），老代码不用改。

## 三、双臂的命名规则

双臂模式下，所有服务和话题带臂前缀：

| 功能 | 左臂 (arm_left, .13) | 右臂 (arm_right, .14) |
|---|---|---|
| 连接/断开 | `/arm_left/tl_driver/connect_arm` | `/arm_right/tl_driver/connect_arm` |
| 上电/断电 | `.../power_on`、`.../power_off` | 同左 |
| 清错 | `.../clear_error` | 同左 |
| 关节运动 moveJ | `.../moveJ` | 同左 |
| 直线运动 moveL | `.../moveL` | 同左 |
| 拖动示教 | `.../get_drag_status` 等 | 同左 |
| 关节状态话题 | `/arm_left/joint_states` | `/arm_right/joint_states` |
| TCP 位姿话题 | `/arm_left/tcp_pose` | `/arm_right/tcp_pose` |
| 臂状态话题 | `/arm_left/arm_status` | `/arm_right/arm_status` |

启动后可查看全部服务：

```bash
ros2 service list | grep tl_driver
```

## 四、常用操作示例

```bash
# 连接（驱动启动后一般自动连，失败时手动调）
ros2 service call /arm_left/tl_driver/connect_arm std_srvs/srv/Trigger

# 上电
ros2 service call /arm_left/tl_driver/power_on std_srvs/srv/Trigger

# 清错（报警后先清错再上电）
ros2 service call /arm_left/tl_driver/clear_error std_srvs/srv/Trigger

# 关节运动（MoveCommand 消息，字段见 tl_ros2_interface/msg/MoveCommand）
ros2 topic pub --once /arm_left/tl_driver/moveJ tl_ros2_interface/msg/MoveCommand ...

# 查看右臂实时关节角
ros2 topic echo /arm_right/joint_states
```

完整服务列表和消息定义见：

- `tl_robot_ros2/src/tl_driver/doc/`（服务与话题说明书）
- `tl_robot_ros2/src/tl_ros2_interface/`（srv/msg 定义）

## 五、技术说明（改动记录 2026-09-10）

- `tl_driver.cpp` 中所有服务/话题名由绝对路径（`/tl_driver/...`）改为相对路径，
  使两个驱动实例可通过 ROS2 命名空间隔离；单臂使用不受影响。
- 双臂 launch：`tl_robot_ros2/src/tl_bringup/launch/tl_tcb605_dual_bringup.launch.py`，
  IP 在该文件中按命名空间分配。
- 双臂 launch **不含 tl_description**（URDF 模型）：两臂关节名相同，同时发布会撞 TF。
  如需在 RViz 同时显示双臂模型，需对 description 做命名空间隔离或合并 URDF（暂未做）。

## 六、灵巧手（万拿 Eco12，非触觉版）控制

手通过臂末端 14 芯航插的 RS485 接入，臂控制器做 Modbus 主站、手做从站。
ROS 包已集成 Modbus 主站接口（`modbus_write` 功能码16 / `modbus_read` 功能码03），
左右手随臂的命名空间区分：`/arm_left/...` 控左手，`/arm_right/...` 控右手。

**固定通信参数**（万拿 Eco12）：从站 ID `1`，控制器串口 `port 2`，`115200/N/8/1`。

**寄存器**：
- `0x0000` 控制字：使能需依次写 `6` → `7` → `15`（进入位置模式）
- `0x0001~0x0007` 关节 1~7 目标行程：0~9000 对应 0~90°（关节3 为 0~5000 对应 0~50°）

> 已实测验证（2026-09-10）：`success=True` 只代表指令已发出，手要动必须先走完使能三步；
> 可用 `modbus_read addr 0 quantity 8` 读回验证，控制字为 15 且关节值正确即正常。

### 左手（192.168.1.13）

```bash
MP="{type: 'RTU', start_addr: true, rtu: {slave_id: 1, port: 2, baudrate: 115200, data_bit: 8, stop_bit: 1, check_bit: 'N'}}"

# 1. 使能三步（每次上电后必做，按顺序）
ros2 service call /arm_left/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 0, data: [6], master_param: $MP}"
ros2 service call /arm_left/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 0, data: [7], master_param: $MP}"
ros2 service call /arm_left/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 0, data: [15], master_param: $MP}"

# 2. 读回验证（应返回 data=[15, ...7个关节值]）
ros2 service call /arm_left/tl_driver/modbus_read tl_ros2_interface/srv/ModbusRead "{master_id: 1, addr: 0, quantity: 8, master_param: $MP}"

# 3. 动作：张开
ros2 service call /arm_left/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 1, data: [0, 0, 0, 0, 0, 0, 0], master_param: $MP}"
#    半握（测试用；全握 = [9000,9000,5000,9000,9000,9000,9000]）
ros2 service call /arm_left/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 1, data: [4500, 4500, 2500, 4500, 4500, 4500, 4500], master_param: $MP}"
```

### 右手（192.168.1.14）

```bash
MP="{type: 'RTU', start_addr: true, rtu: {slave_id: 1, port: 2, baudrate: 115200, data_bit: 8, stop_bit: 1, check_bit: 'N'}}"

# 1. 使能三步（每次上电后必做，按顺序）
ros2 service call /arm_right/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 0, data: [6], master_param: $MP}"
ros2 service call /arm_right/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 0, data: [7], master_param: $MP}"
ros2 service call /arm_right/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 0, data: [15], master_param: $MP}"

# 2. 读回验证
ros2 service call /arm_right/tl_driver/modbus_read tl_ros2_interface/srv/ModbusRead "{master_id: 1, addr: 0, quantity: 8, master_param: $MP}"

# 3. 动作：张开
ros2 service call /arm_right/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 1, data: [0, 0, 0, 0, 0, 0, 0], master_param: $MP}"
#    半握
ros2 service call /arm_right/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 1, data: [4500, 4500, 2500, 4500, 4500, 4500, 4500], master_param: $MP}"
```

### 双手同时张开（一条命令）

```bash
MP="{type: 'RTU', start_addr: true, rtu: {slave_id: 1, port: 2, baudrate: 115200, data_bit: 8, stop_bit: 1, check_bit: 'N'}}"
for arm in arm_left arm_right; do
  ros2 service call /$arm/tl_driver/modbus_write tl_ros2_interface/srv/ModbusWrite "{master_id: 1, addr: 1, data: [0, 0, 0, 0, 0, 0, 0], master_param: $MP}"
done
```

参考：天链官网《万拿Eco12灵巧手》集成说明 https://www.tlibot.com/arm/425.html ；
更完整寄存器表见万拿《灵巧手通讯协议》（可向厂家索取）。

## 七、排障

| 现象 | 排查 |
|---|---|
| 启动报 failed to connect | 先 ping 对应 IP；确认机械臂控制器已上电、网线插好 |
| 服务调用无响应/调错臂 | 确认服务名前缀（arm_left/arm_right）没写错 |
| 只有一套服务 | 确认用的是 `tl_tcb605_dual_bringup.launch.py` |
| 改了驱动代码不生效 | `colcon build --packages-select tl_driver --symlink-install` 后重 source |
