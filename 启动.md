# KYBOT 差速底盘 启动命令

注意！此次的nav2目前使用的是三维雷达转scan方式  但小车过高 此方式不可行 务必更改

每个终端第一条都要先 source（开了新终端就执行一次）：

```bash
source /home/kybot/kybot_ros2_ws/install/setup.bash
```

## 一、正常巡检/导航启动顺序（按顺序，每步一个终端）

### 1. 雷达驱动（MID360s，发 /livox/lidar 和 /livox/imu）

```bash
ros2 launch livox_ros_driver2 msg_MID360s_launch.py
```

### 2. 底盘（/odom 话题 + 订阅 /cmd_vel）

```bash
ros2 launch ros_arduino_python arduino.launch.py
```

注意：
- 串口已做别名 `/dev/ttyARDUINO`（CH340，规则在 `/etc/udev/rules.d/99-ttyarduino.rules`，驱动开机自启，不用管）
- 启动前确认没有残留的 arduino_node：`pgrep -af arduino_node`，有就先 `kill`。
  **两个节点同时抢串口会疯狂报 "Encoder exception"**
- arduino.yaml 已设 `publish_tf: false`（TF 由 EKF 独占广播）、`use_imu_onboard: false`（IMU 用雷达的）

### 3. 定位（FAST_LIO + EKF）

```bash
ros2 launch fast_lio_localization velodyne_localization.launch.py use_sim_time:=false config_file:=mid360s.yaml
```

内含：
- EKF：融合轮式里程计 /odom + 雷达 IMU /livox/imu，广播 odom->base_link
  （配置：`FAST_LIO_LOCALIZATION_ROS2/config/ekf_config.yaml`。
   2026-09-10 修过两个坑：/odom 协方差全 0 → 已在 base_controller.py 补上；
   process_noise_covariance 矩阵错位 → 已恢复。EKF 一旦报 NaN "Critical Error" 必须重启它，自己恢复不了）
- base_link->livox_frame 静态 TF（雷达安装有偏移时在 launch 文件里改）

### 4. Nav2 导航（差速底盘：RPP 控制器 + Smac Hybrid-A*）

```bash
ros2 launch nav2_bringup bringup_launch.py use_sim_time:=false \
  map:=/home/kybot/kybot_ros2_ws/my_map.yaml \
  params_file:=/home/kybot/kybot_ros2_ws/nav2_params/param_top_diff_bs.yaml
```

启动后在 RViz 里先给初始位姿（2D Pose Estimate），再发导航目标。

## 二、建图流程（换新环境时）

```bash
# 1. 建图（雷达静止几秒后再动）
ros2 launch fast_lio mapping.launch.py config_file:=mid360.yaml

# 2. 结束前另开终端保存 pcd（保存在启动 launch 的当前目录 test.pcd）
ros2 service call /map_save std_srvs/srv/Trigger

# 3.切片
ros2 launch pcd2pgm pcd2pgm_launch.py
ros2 run nav2_map_server map_saver_cli -f my_map
```


## 三、改了代码重新编译

```bash
cd /home/kybot/kybot_ros2_ws
colcon build --packages-select fast_lio_localization --symlink-install
source install/setup.bash
```

（ros_arduino_python 直接改 install 目录下的 py 文件即可生效，不用编译）

## 四、常用排障

```bash
ros2 topic echo /cmd_vel            # 导航激活时应有速度指令（Nav2 直接发到这里）
ros2 topic echo /odom --once        # 协方差不应该是全 0
ros2 run tf2_ros tf2_echo odom base_link   # EKF 正常时有数值，NaN 就是 EKF 又炸了
pgrep -af arduino_node              # 应该只有一个进程
```



