# KYBOT 定位链路问题清单与优化建议

> 对象：`kybot_ros2_ws` 中的 **FAST-LIO 定位链路**（`FAST_LIO_LOCALIZATION_ROS2` + `livox_ros_driver2` 补偿 + `kybot_base` 里程计 + `robot_localization` EKF + `nav2_params`）
> 依据提交：`95151a0`（当前 HEAD）
> 方法：**静态审计**（逐行读代码 / 配置 / launch，结合 git 历史与 `启动.md`），未在机器人上运行验证
> 每条问题的证据都给了 `文件:行号`，并标注证据强度：**【已核实】**＝已在代码中确认；**【推断】**＝由代码逻辑推导，建议实测确认；**【待实测】**＝无法静态判定

---

## 0. 摘要：最该先动的 8 条

| 编号 | 一句话 | 影响面 | 证据强度 |
|---|---|---|---|
| **P0-1** | TF 树里的 `base_link` 恒等于雷达/IMU 位姿，全局位姿系统性偏 **水平约 0.40 m（随朝向旋转）** | Nav2 到点精度、footprint 安全边界、局部/全局代价地图一致性 | 已核实 |
| **P0-2** | `odom` **同名异系**：点云标的是 LIO 的 odom，TF 里的 odom 是 EKF 的 odom → `/scan_fe` 障碍位置随两个里程计的分歧漂移 | Nav2 costmap 障碍位置 | 已核实 |
| **P0-3** | `use_sim_time` 默认 `true`，而 `pointcloud_to_laserscan`/RViz 又没接这个参数 → 忘传 `false` 时整链静默冻结 | 全链路启动 | 已核实 |
| **P0-4** | `preprocess.cpp:183-186` 运算符优先级 bug → `blind: 0.5` 在你们实际走的 avia 路径上**基本不生效** | 车体/支架点进入点云 | 已核实 |
| **P0-5** | `/Odometry` 协方差在 `publish()` 之后才填（滞后一帧，首帧全 0）；`/localization` 协方差与 twist 全 0 | 任何用协方差的消费者 | 已核实 |
| **P0-6** | 定位配置下 `/map_save` **必然返回失败**；且建图/定位两套配置的 PCD 路径不一致 | 建图流程 | 已核实 |
| **P0-7** | RViz 配置文件名拼错，布局加载不了（而你们的操作流程依赖 RViz 给初始位姿） | 人工操作 | 已核实 |
| **P0-8** | 驱动做了 28.38° 倾斜补偿，但 `extrinsic_T` 没有一起旋转（差约 **2.5 cm**）；且两套配置的外参值本身不一致 | 雷达-IMU 杆臂 | 已核实（数值）/ 待实测（哪套对） |

**紧急但属于"仓库状态"而非代码**：见 §1.2（索引被清空）、§1.3（`S2.hpp` 在 git 里是 UTF-16，重新 clone 会编译失败）。

---

## 1. 仓库与 git 历史现状

### 1.1 事实梳理

本仓库（origin `saiyuhang123/kybot_ros2_ws`）共 4 个提交，**只有 `e6e290d` 真正改过这个定位包**：

| 提交 | 说明 | 是否动定位包 |
|---|---|---|
| `a1a2e20` | 初始提交（991 文件，整工作空间一次性纳入） | ✅ 59 个文件整包 vendor |
| `e6e290d` | 雷达底座倾斜补偿 + 整机集成 | ✅ 4 个文件（ekf_config.yaml / mid360s.yaml / velodyne_localization.launch.py / transform_fusion.py） |
| `f7cfaef` | 多设备集成 | ❌ |
| `95151a0` | 小调整（含 `启动.md` 加入"三维转 scan 不可行"记录） | ❌ |

**重要**：真正的大改造（`global_localization.py` 的向量化解析 / 两级 ICP / 法向量预计算 / 可视化按订阅者跳过 / scan 与 odom 原子配对）在**初始提交之前就完成了**，git 历史里看不到过程；`config/mid360s.yaml` 里"原来 1.0 / 原来 0.9 / 原来 0.0001"这类调参记录也只有结果没有过程。所以**只用 git 历史评价这个包会严重低估已有工作量**。

### 1.2 ⚠️ 索引被清空（危险状态）

```
git ls-files          → 0 个文件（索引为空）
git status --porcelain → 1528 条，其中全部为 staged delete + 工作区文件 untracked
git ls-tree -r HEAD   → 1511 个文件（HEAD 内容完好）
```

这是 `git rm -r --cached .` 之后又 `add` 过的典型状态。风险：

- `git checkout .`、`git stash`、误操作的 `git reset --hard` 都可能丢东西；
- 从此**分不清哪些是本地改动**（工作区相对 HEAD 只差 1 个文件，但你没法从 `git status` 一眼看出）；
- 无法再做有意义的 code review / 回滚。

**处理**：`git add -A` 重建索引 → 提交一次"恢复索引"的提交（见 §6 第 1 批）。

### 1.3 ⚠️ `S2.hpp` 在 git 里是 UTF-16LE，重新 clone 会编译失败

| 版本 | 编码 | 大小 |
|---|---|---|
| HEAD（`include/IKFoM_toolkit/mtk/types/S2.hpp`） | **UTF-16LE + BOM（FF FE）**，每字符后带 `0x00` | 19944 B |
| 当前工作区 | UTF-8（无 BOM） | 9971 B |

两者**解码后内容完全一致**（9655 字符，逐字符比对一致），说明有人用编辑器打开重存过，但**这个修正没有提交**。后果：一旦在机器人上重新 `git clone`（或队友克隆），拿到的是 UTF-16 文件，gcc 在文件开头遇到 BOM 会报 `stray '\377' in program`，**`fast_lio_localization` 直接编不过**。

**处理**：把工作区的 UTF-8 版本单独提交，commit message 写明"修正 S2.hpp 编码（UTF-16LE → UTF-8），否则新克隆无法编译"（见 §6 第 1 批）。

---

## 2. P0 —— 正确性问题（建议立刻修）

### P0-1 TF 里的 `base_link` 其实是雷达/IMU 原点（全局位姿系统性偏移）

**证据**

| 事实 | 位置 |
|---|---|
| `/Odometry` 的位姿来自 `state_point.pos` / `geoQuat`，即 **IMU body 位姿**（FAST-LIO 状态定义在 IMU 上） | `src/laserMapping.cpp:615-626` |
| 但 `child_frame_id` 写的是 `"base_link"` | `src/laserMapping.cpp:631-632` |
| FAST-LIO 自己的 TF 广播整段被注释（TF 里没有它的声音） | `src/laserMapping.cpp:648-659` |
| `transform_fusion` 把 EKF 的 `odom→base_link`（＝**真实车心**，因为 EKF 用静态 TF 把 IMU 变换到了 base_link）当成"odom→body"用 | `scripts/transform_fusion.py:133-135, 145` |
| 静态 TF `base_link→livox_frame` = (0.40, 0, 0.22)，旋转 0 | `launch/velodyne_localization.launch.py:103-108` |
| EKF 的 `odom→base_link` 是唯一发布者 | `config/ekf_config.yaml:15, 21`；`kybot_base/.../arduino.yaml`（`publish_tf: false`） |

**推导（代数上是精确抵消，不是近似）**

记 `B`=真实车心、`I`=雷达/IMU 原点、`O`=EKF 的 odom、`M`=map。

```
T_M←I               = T_map_to_body = T_map_to_odom_fl ⊗ T_odom_fl_to_body   # transform_fusion.py:126-128
T_M←O(发布)         = T_M←I ⊗ inv(T_O←B)                                     # transform_fusion.py:145
TF 树合成：T_M←B(树) = T_M←O(发布) ⊗ T_O←B(EKF) = T_M←I ⊗ inv(T_O←B) ⊗ T_O←B = T_M←I
```

**结论：无论 EKF 跑得多准，TF 树里的 `base_link` 恒等于雷达/IMU 的全局位姿**，EKF 那个"真车心"位姿被 `inv()` 用进去又被 TF 树乘回来，正好消掉。

**偏差量**：`T(B←I)` 的平移（IMU 原点在车体系下的坐标）

- `base_link→livox_frame`：水平 **0.40 m**、竖直 0.22 m（确定值）
- 再叠加 MID360 内部 `livox_frame→IMU` 约 5 cm（`extrinsic_T` 的模长；方向待实测，见 P0-8）
- 合成后大致：**水平 0.36~0.44 m、竖直 0.19~0.25 m**

**它是 body 系固定向量 `d`，所以：上报位置 = 真实车心 + R(yaw)·d**

**影响**

1. **原地转圈时上报位置自己画半径约 0.40 m 的圆**（车心没动也会动）→ 这**不是定位漂移**。若之前是靠肉眼看 `/localization` 或 RViz 判断"转圈漂不漂"，观察结果里混了这个伪影，修完值得重跑一次转圈测试。
2. **到点差约 0.40 m**：规划起点、到点判定都用 `map→base_link`，车心会停在目标前约 0.4 m（车半长 0.55 m）。
3. **footprint 整体前移 0.40 m**：`[±0.55, ±0.37]` 以"base_link"为中心绘制（`nav2_params/param_top_diff_bs.yaml:73`），实际覆盖范围从车尾 −0.55 缩到约 −0.15 → **真实车尾约 0.4 m 不在 costmap 的机器人轮廓内**，贴墙/倒车/原地转向有碰撞风险。
4. **局部与全局代价地图对机器人位置的认知不一致**：局部（`global_frame: odom`）＝真实车心；全局（`global_frame: map`）＝雷达位置。
5. 连带：`map→livox_frame` 又比真实雷达位置向前多偏 0.40 m（累计约 0.76 m）。
6. **RViz 里看不出来**：整条 TF 链自洽，点云/地图/机器人模型都按同一套 TF 摆放，怎么调都"看着对"——这是它能活到现在的原因。

**修法**

- **方案 A（Python，不重编译，建议先做）**：在 `scripts/transform_fusion.py:145` 的乘积中间插入 `T(I←B)`，即 `base_link→livox_frame` 平移取负：

  ```python
  T_imu_from_base = np.eye(4)
  T_imu_from_base[:3, 3] = (-0.40, 0.0, -0.22)   # 想精确到厘米再加 livox_frame→IMU 的约 5 cm
  T_map_to_odom = self._mat_mul(self._mat_mul(T_map_to_body, T_imu_from_base),
                                self._inv_mat(T_odom_ekf_to_body))
  ```

  顺手把 `/localization` 的 `child_frame_id` 从 `'body'`（全系统没人发布这个 frame）改成 `'base_link'`：`scripts/transform_fusion.py:173`。

- **方案 B（C++，更根本，需 `colcon build`）**：在 `src/laserMapping.cpp:615-626` 的 `set_posestamp` 里把 IMU 位姿换算成车心位姿：
  `p_base = state_point.pos + state_point.rot * t_imu_from_base`（旋转不变，水平补偿后 body 与 base 同姿态）。这样 `/Odometry`、`/path` 全部说实话。注意 `global_localization.py:199` 用 `/Odometry` 当 body 位姿做 FOV 裁剪，0.4 m 误差对裁剪无影响，加注释即可。

**验证**

1. 原地缓慢转一圈 + `ros2 run tf2_ros tf2_echo map base_link`：修前 (x,y) 画半径约 0.4 m 的圆；修后车心不动就应该不动（残余 < 5 cm）。
2. 在 map 里发目标点让车到点，卷尺量车心到目标实际距离：修前约差 0.4 m。
3. 把车心对准地图上可辨认的地标，在 RViz 量机器人模型中心与地标的偏差：修前约 0.4 m 且随朝向变化。

**前提/注意**

- 前提是底盘 `/odom` 里的 `base_link` 与静态 TF 用的 (0.40, 0.22) 是同一个物理原点；否则会在 0.4 m 之上再叠一层（用验证 1 的圆心位置可判断）。
- `static_transform_publisher` 的位置参数顺序是 `x y z yaw pitch roll frame_id child_frame_id`（**yaw pitch roll，不是 rpy**）。当前三个旋转都是 0 所以无影响，将来若加旋转别写错。

---

### P0-2 `odom` 同名异系 → `/scan_fe` 几何随两个里程计的分歧漂移

**证据**

| 事实 | 位置 |
|---|---|
| `/cloud_registered` / `/cloud_effected` / `/path` 的 `frame_id = "odom"`，指的是 **FAST-LIO 自己的 odom 系** | `src/laserMapping.cpp:508, 577, 599, 666` |
| TF 里的 `odom` 由 EKF 发布，是 **EKF（轮式里程计积分）的 odom 系** | `config/ekf_config.yaml:21`；`launch:88-98` |
| `pointcloud_to_laserscan` 用 TF 把标着 `odom` 的点云变到 `base_link` | `launch/velodyne_localization.launch.py:113-134` |
| `/scan_fe` 同时喂局部（`global_frame: odom`）与全局（`global_frame: map`）代价地图 | `nav2_params/param_top_diff_bs.yaml:65, 90, 115, 128` |

**到底哪个点云在"借用"`odom`？**（当前定位配置下，实际只有 `/cloud_registered` 一个在跑）

| 话题 | 声明的 frame_id | 当前是否发布 | 位置 |
|---|---|---|---|
| `/cloud_registered` | `odom` | ✅ 10 Hz（`scan_pub_en: true`）→ 喂 `/scan_fe` → Nav2 costmap | `src/laserMapping.cpp:508, 1074` |
| `/cur_scan_in_map` | `odom`（原样重贴标签） | ✅ 但只是 `/cloud_registered` 的可视化副本，无订阅者时跳过 | `scripts/global_localization.py:142` |
| `/cloud_registered_body` | `base_link`（点实际在 IMU 系，**另一处误标**） | ❌ `scan_bodyframe_pub_en: false` | `src/laserMapping.cpp:560, 1075` |
| `/cloud_effected` | `odom` | ❌ `publish.effect_map_en` 未配置（默认 false） | `src/laserMapping.cpp:577, 1076` |
| `/Laser_map` | `odom` | 仅建图配置（`publish.map_en: true`） | `src/laserMapping.cpp:599, 1113` |
| `/path` | `odom` | ❌ `path_en: false` | `src/laserMapping.cpp:666, 877` |

**为什么会"借"**：frame 名在 `src/laserMapping.cpp` 里是**硬编码字面量**（没有参数入口），而这个字面量恰好选了 `"odom"`；同时 LIO 的世界系从未以任何名字注册进 TF 树（`:648-659` 广播被注释、`:937/:1143` 的 broadcaster 建了不用）。名字来源是上游 FAST-LIO 的 `camera_init`/`body` 命名被整体替换成 Nav2 习惯的 `odom`/`base_link`（`:631-632` 的 `child_frame_id` 也是这么改的；初始提交里已是如此）。**改名让接口"看起来"兼容，但改名既不会在树里建立 frame，也不会让 `W_L` 等于 EKF 的 odom。**

**为什么这比"名字取得难看"严重得多**：如果它老实叫 `camera_init`，`pointcloud_to_laserscan` 的 TF 查找会**当场失败**（scan 根本发不出来），上线第一天就会暴露；叫 `odom` 则恰好能在树里**查到**，只是查到的是另一个系——**把一次本该响亮的失败，换成了一次安静的错答**。这就是它藏了这么久的原因。

**机理**：点云物理上在 LIO 系 `W_L`，却按 `frame_id="odom"` 被 TF 解释成 EKF 系 `O_E`。两者只在"轮式 odom ≈ LIO odom"这个假设下近似相等；轮式打滑/LIO 漂移会让它们分离，而**唯一的绝对观测（1 Hz ICP）只修正 `map→odom`，不会把 `O_E` 拉回 `W_L`**，所以这个差值**无界增长**。误差正好等于两个里程计之差，直接进 costmap 的障碍位置。

这也和你们自己写在 `启动.md:3` 的判断一致：「此次的 nav2 目前使用的是三维雷达转 scan 方式 但小车过高 此方式不可行 务必修」。

**为什么包里的 `map→odom` 广播修不掉这个问题？（最容易误解的一点）**

包里确实有"坐标重新绑定"，但**全包只有两处**，且都不是把 LIO 系接到 odom 上：

| 动作 | 位置 | 实际作用 |
|---|---|---|
| 广播 TF `map→odom` | `scripts/transform_fusion.py:155-156, 164` | 把 **EKF 的 odom 系**摆到 map 里的正确位置。这是**全包唯一的 TF 发布** |
| 重贴点云 `frame_id = 'odom'` | `scripts/global_localization.py:142` | 输入本来就是 `odom`，**odom→odom 的空操作**；且只发到可视化话题 `/cur_scan_in_map`，不做任何点变换 |
| LIO 自己的 TF 广播 | `src/laserMapping.cpp:648-659`（注释）、`:937, 1143`（建了 broadcaster 但从不使用） | **完全没有发布** |
| `base_link→livox_frame` 静态 TF | `launch/velodyne_localization.launch.py:103-108` | 与 odom 无关 |
| `/localization` 的 `child_frame_id = 'body'` | `scripts/transform_fusion.py:173` | 树里没有 `body` 这个 frame，消费者无法用 |

也就是说：**LIO 的世界系 `W_L` 在 TF 树里没有任何一条边**——它只是把名字写成 `"odom"`，"借用"了 EKF 那个 odom 在树里的位置。

而 `transform_fusion` 的 `map→odom` 到底绑定了什么：

```
T_M←O = T_M←I ⊗ inv(T_O←B)        # transform_fusion.py:145
```

它的效果是让 `map→odom→base_link` 这条链**恰好复现 LIO 的 map 位姿**（所以 `base_link` 是对的），代价是 **odom 系被"拉走"**——odom 相对 map 的位置随 EKF 漂移和 ICP 修正一起被推着走。

**这份代码的取舍是：牺牲 odom 系，保全 base_link。而点云恰恰是按 `odom` 这个名字被解读的 → 被牺牲的那一方正好是点云。** 两个 odom 系的关系是：

```
T_{W_L←O} = T_{W_L←B}(LIO 的 /Odometry) ⊗ inv(T_{O←B})(EKF 的 TF)
```

这个量**在代码里任何地方都没有被计算或发布**，而它恒等于 `/scan_fe` 里障碍位置的偏移量。

**一句话总结：TF 树是对的，名字是错的。** 树里 `map→odom→base_link` 三个 frame 之间的关系完全自洽（这条链精确复现 LIO+ICP 的全局位姿，且 `odom→base_link` 由 EKF 发布、连续无跳变），所以**树里那个 odom 本身没有毛病**——它就是标准 odom 该有的样子（连续、允许相对 map 漂移，跳变交给 `map→odom` 去吸收）。真正错的是：**点云自称住在 `odom`，实际住在只借了这个名字、却不在树里的 `W_L`**。

补充一点容易混的地方：**"自洽"不是这个公式带来的**。TF 树只要边存在，`map→odom` 取任何值（哪怕是单位阵）都自洽；公式换来的是**内容正确**——它把 LIO 与 EKF 的差异全部搬进 `map→odom` 这条边，使合成结果恰好等于 LIO 的全局位姿。即：**`map→base_link` 的值完全由 LIO+ICP 决定，EKF 一点都不参与；EKF 只决定"中间站 odom 摆在哪"。** 反证：`transform_fusion.py:147` 的 fallback 分支（EKF 不可用时直接把 ICP 结果当 `map→odom` 发）树照样自洽，但 `base_link` 就错了。

**一分钟验证**

```bash
# 两个都自称 odom->base_link，但属于两个不同的 odom 系
ros2 run tf2_ros tf2_echo odom base_link             # EKF 的
ros2 topic echo /Odometry --field pose.pose --once   # LIO 自报的
```

启动瞬间两者应接近（两个 odom 系几乎在同一位置起算）；跑几分钟后若数值明显分开，分开多少，`/scan_fe` 里的障碍就偏多少。

**后果（按严重度）**

1. **局部代价地图（`global_frame: odom`）障碍位置错 → 碰撞风险最大。** 机器人自身位姿来自 EKF（正确），而障碍来自"用 EKF 变换过的 `W_L` 点云"，两者相对几何的偏差 = `T_{W_L←O}`。表现为贴墙/擦角、对"鬼影障碍"无谓停车、膨胀层偏移。注意**角度分量会被距离放大**：分歧 3° 时，5 m 处横向错 26 cm、10 m 处错 52 cm。
2. **全局代价地图（`global_frame: map`）实时障碍与静态地图错位。** 同一个错变换把 scan 投到 map 上、叠加到 `pcd2pgm` 出来的静态图上 → 实时的墙与地图上的墙对不上，规划器对着错位的障碍绕行或漏判。
3. **RViz 里"机器人模型 vs 点云"随运行时间慢慢错开 → 容易被误读成"LIO 漂了"。** 机器人模型走 TF（正确），点云走 `frame_id`（被按 odom 解释）→ 两者错开；而 LIO 其实是好的。**这条最阴险：它会让人去调 LIO 参数（`filter_size_surf`、`b_gyr_cov` 之类），真正的问题却只是坐标系标签。**
4. 将来任何新模块只要按 `frame_id` 消费这些点云，就会直接拿到错坐标，并且同样无声。

**什么时候最严重**：冷启动初期两者从同一点起算、分歧 ≈ 0（所以"刚开机测试一切正常"是典型表现）；之后随里程**单调增长、不会自愈**（ICP 只修 `map→W_L`，既不碰 EKF 也不碰轮式 odom）。放大因素：打滑（湿地/地毯/急转/**原地转圈**——差速轮原地转时编码器角度最不可靠，这也解释了为什么"转圈时问题最明显"）、长距离巡检、轮径/轮距标定误差。

**量级（数量级推算，非实测）**

评估规则：**分歧 ≈ 轮式里程计的位置相对误差 ε × 里程 S**（ε 典型 0.5%~3%，取决于轮径/轮距标定、地面、打滑；LIO 侧漂移量级更小，通常落在差值的下一位）。

| 条件 | 两个里程计的分歧 | 局部 costmap 障碍错位 |
|---|---|---|
| 冷启动后头几十米（两个 odom 从同一点起算） | 厘米级 | 基本无感——**这就是它一直没被发现的原因** |
| ε≈1%、S=100 m | ≈1 m | 明显：贴墙、过门、绕障都会偏 |
| ε≈2%、S=100 m，或一次明显打滑 | ≈2 m 以上 | 障碍场整体错位，效果等同于"机器人在图里站错了位置" |
| 原地转圈 / 急转（差速轮打滑） | 以角度分歧为主，且被距离放大（3° → 5 m 处错 26 cm、10 m 处错 52 cm） | 远处障碍错得最厉害 |

**真实值必须实测**：用前面"一分钟验证"的两条命令算出 `T_{W_L←O}`，它的平移量与角度就是障碍偏移量。

**不影响什么**（防止误调参）：不影响 `map→base_link`（全局定位输出由 LIO+ICP 决定，P0-2 不碰它）；不影响 ICP 本身；不影响 EKF 滤波（它只吃 `/odom` 与 `/livox/imu`）。**它坏的是"感知 → 代价地图"这一段，不是"定位"这一段。**

**怎么复现**：走一个闭环回到出发点，对比两件事——(a) 全局位姿应基本回到起点（说明 LIO+ICP 是好的）；(b) RViz 里点云相对静态地图/机器人模型却错开了一个量 → 那个错开量就是 `T_{W_L←O}`。也可直接用前面"一分钟验证"的两条命令对比。

**防呆建议**：加一个常驻自检（1 Hz 计算 `T_{W_L←O}` 的平移与角度），超阈值（如 0.2 m / 2°）就告警——**这是把"安静的错答"改回"响亮的失败"的唯一低成本手段**。

**修法（三选一，按彻底程度排序）**

1. **最小改动（推荐；完整方案与代码见 §9）**：新增一个 ~55 行的小节点，用 **LIO 自身位姿**（`/Odometry`）把 `/cloud_registered` 转到 `base_link` 后另发一路，`pointcloud_to_laserscan` 改订阅它并把 `target_frame` 留空（不再依赖 TF）。全程不做 TF 查询，几何偏差恒为 0；且是旁路改动，可一键回滚。
2. **改名**：把 LIO 世界系从 `odom` 改名 `camera_init`（`laserMapping.cpp:508, 631`），并解除 `:648-659` 的 TF 广播注释发布 `camera_init→body`，再补 `base_link↔body` 的静态 TF。彻底但改动面大。
3. **换感知源**：改二维雷达（或直接把 `/cloud_registered` 以 `PointCloud2` 喂 costmap）。**注意**：换二维雷达能消掉本条的漂移，但 **P0-1 的 0.4 m 与 P0-3 的 `use_sim_time` 依然存在**（详见 §5 讨论）。

---

### P0-3 `use_sim_time` 默认 `true`，且部分节点没接这个参数

- `launch/velodyne_localization.launch.py:24-27` 默认 `use_sim_time:=true`；而 `mapping.launch.py:26` 默认 `false`（两者相反）。
- 传给了：`fast_lio_mapping` / `global_localization` / `transform_fusion` / `global_map_publisher` / `ekf_node`。
- **没传给**：`pointcloud_to_laserscan`（`:113-134`）、`livox_static_tf_node`（`:103-108`）、`rviz`（`:136-142`）。
- 危害分级：ROS 2 的 `use_sim_time` 内建参数**默认 `false`**，所以只要显式传 `false`（`启动.md:34` 是这么做的）就没问题；**真正危险的是默认值本身**——忘传参数时，LIO 的 100 Hz 主循环、ICP 定时器、50 Hz 融合、EKF 全部按仿真时间走（无 `/clock` 时时间为 0，定时器不触发），整链静默失效且不报错。
- 未接该参数的两个节点只在 `use_sim_time:=true`（仿真、带 `/clock` 播包）时才不一致：`pointcloud_to_laserscan` 内部 `tf2_ros::MessageFilter` 的超时/过期逻辑会算错，导致 scan 时有时无；RViz 会刷 "message filter dropping message"。

**修法**：把默认值改成 `false`，并给全部节点统一注入同一个值（用 `SetParameter`/`IncludeLaunchDescription` 或逐个节点补 `{'use_sim_time': use_sim_time}`）。**若确定不用 3D→scan，直接从 launch 删掉该节点**（Jetson 上少一个 10 Hz 全点云转换进程）。

---

### P0-4 `preprocess.cpp:183-186` 运算符优先级 bug → `blind` 在实车路径上基本不生效

```cpp
if ((abs(pl_full[i].x - pl_full[i-1].x) > 1e-7)
    || (abs(pl_full[i].y - pl_full[i-1].y) > 1e-7)
    || (abs(pl_full[i].z - pl_full[i-1].z) > 1e-7)
    && (x*x + y*y + z*z > blind*blind))     // ← && 只作用于第三个条件
```

`&&` 优先级高于 `||`，实际语义是 `A || B || (C && D)`。相邻 Livox 点的 x 或 y 几乎总有变化 → **绝大多数点绕过盲区判断直接被塞进 `pl_surf`**，`D`（真正的盲区过滤）只在 x、y 完全相同的极少情况下才生效。

- 位置：`src/preprocess.cpp:183-186`，位于 **`avia_handler`**（函数范围 `:95-194`），而 `process(CustomMsg)` 无条件走 `avia_handler`（`:47-51`），配置又是 `lidar_type: 1` + `feature_extract_enable: false`（走 `:166-193` 的 `else` 分支）→ **这是你们实车正在跑的路径**。
- 影响：`config/mid360s.yaml:40` 的 `blind: 0.5`（注释写"若车身/支架进入视场被采到，调大此值"）**调了也没用**；车体/支架点会被采进点云参与配准。
- 来源：这是上游 FAST-LIO 的原始写法（移植版继承），不是本次改造引入的。
- **建议**：加一对括号 `|| ((dz > 1e-7) && (range2 > blind*blind))`，或直接改成独立的 `if (range2 < blind*blind) continue;`。改完需要重编译并重新观察一次建图效果（点云会更干净，近处地面点也会少一些）。

---

### P0-5 协方差与时序小 bug

| 问题 | 位置 | 影响 |
|---|---|---|
| `pubOdomAftMapped->publish()`（`:635`）**在**填 `pose.covariance`（`:637-646`）**之前**，而 `odomAftMapped` 是全局对象 | `src/laserMapping.cpp:629-646` | 发布的是**上一帧**的协方差；首帧全 0。任何信任 `/Odometry` 协方差的消费者会被误导 |
| `/localization` 的 `pose.covariance`、`twist` 从未赋值（twist 直接拷 `/Odometry`，而它也没填 twist） | `scripts/transform_fusion.py:170-179` | 下游视为"无限精确" |
| `/map_to_odom` 缺 `child_frame_id`，也无协方差 | `scripts/global_localization.py:183-191` | 同上 |
| `publish_initial_pose.py` 创建 publisher 后**立即** publish，仅 `spin_once(1.0)` 就退出 | `scripts/publish_initial_pose.py:11-12, 40` | DDS 尚未完成匹配，消息大概率丢失 |
| `/initialpose` 的协方差被完全忽略，且它被当作 `map→odom` 使用（见 P1-3） | `scripts/global_localization.py:115-125, 199` | 重定位语义错 |

**修法**：把协方差填充移到 `publish()` 之前（一行位置调整）；`/localization` 至少填对角线（可用 ICP 的 `inlier_rmse` 与 LIO 的 `P` 组合），或明确置 `-1`/大值表示不可用；`publish_initial_pose.py` 改为重复发布若干次或 `transient_local`。

---

### P0-6 `/map_save` 在当前定位配置下必然失败；建图/定位配置路径不一致

| 事实 | 位置 |
|---|---|
| `map_save_callback` 只在 `pcd_save_en == true` 时保存，否则回 `"Map save disabled."` | `src/laserMapping.cpp:1116-1130` |
| 定位配置 `mid360s.yaml:62` 是 `pcd_save_en: false` → **服务必然返回失败** | `config/mid360s.yaml:61-64` |
| 即便打开 `pcd_save_en`，`pcl_wait_pub` 也只在 `publish_map()` 里累积，而它需要 `publish.map_en: true`（`mid360s.yaml` 的 `publish` 段没有 `map_en`，默认 `false`） | `src/laserMapping.cpp:1111-1114, 840` |
| 建图配置 `mid360.yaml` 有 `map_en: true` + `pcd_save_en: true` + `map_file_path: "./test.pcd"`（相对启动目录） | `config/mid360.yaml:10, 42, 48` |
| 定位配置要的 PCD 是绝对路径 `/home/kybot/fast_lio2_ws/test.pcd`（**另一个 workspace**） | `config/mid360s.yaml:16` |
| `map_pub_timer_` 每 1 s 把**累积**地图整体重发到 `/Laser_map`，`pcl_wait_pub` 无上限增长 | `src/laserMapping.cpp:943-944, 581-600` |

**影响**：`启动.md` 的建图流程（`mapping.launch.py config_file:=mid360.yaml` + `ros2 service call /map_save`）本身是对的，但**很容易在定位配置下误用**，且"建图输出到启动目录的 `test.pcd`"与"定位读 `/home/kybot/fast_lio2_ws/test.pcd`"需要人工搬运，任一步漏掉就会导致定位端收不到 `/global_map`。

**建议**：定位配置增加 launch 参数 `map_file`（默认指向与建图相同的路径）+ 启动时校验文件存在；`global_map_publisher` 读失败时**直接退出**而不是只打日志（见 P1-5）；建图长时段运行时关闭 `map_en`（只靠 `pcd_save`）以避免每秒重发整幅地图。

---

### P0-7 RViz 配置文件名拼错

`launch/velodyne_localization.launch.py:16` 找的是 `rviz/fastlio_localiztion.rviz`（**少一个 a**），实际文件是 `rviz/fastlio_localization.rviz` → RViz 启动但加载不到布局。而你们的工作流恰恰依赖 RViz 给初始位姿（`启动.md:44`）。一行修复。

---

### P0-8 倾斜补偿后 `extrinsic_T` 未同步旋转；两套配置外参不一致

驱动在同一提交里开启了点云与 IMU 的水平补偿（`livox_ros_driver2/launch_ROS2/msg_MID360s_launch.py:32-36`，`mount_pitch: 0.4954` ≈ 28.38°，实现见 `livox_ros_driver2/src/lddc.cpp` `SetMountCompensation` / `RotateVec`）。

点云与 IMU 被同一个 `R_m` 旋转，所以 `extrinsic_R` 保持单位阵是对的（`mid360s.yaml:51-53`），**但平移项必须一起转**：

```
T'_LI = R_m · T_LI
R_m = Ry(0.4954)（mount_roll = mount_yaw = 0）
T_LI = (0.04165, 0.02326, -0.0284)   →   T'_LI ≈ (0.0231, 0.0233, -0.0448)
差值 ≈ 2.5 cm
```

另外 **两套配置的外参值本身不一致**：`mid360s.yaml:50` = `[0.04165, 0.02326, -0.0284]`，`mid360.yaml:34` = `[-0.011, -0.02329, 0.04412]`（后者与 Livox 官方 MID360 值一致，仅符号取向不同）。定位配置里 `extrinsic_est_en: false`，所以用错就是**永久性系统误差**。

**建议**：用 LI-Init 或一次静止+旋转标定确认自己这台的外参；确认后按 `R_m·T` 填进定位配置；并在 `mid360s.yaml` 注释里写清"该值已按 mount_pitch 旋转"。

---

## 3. P1 —— 精度与鲁棒性

### P1-1 ICP 的输入早就被削得太狠（精度上限约 0.5 m）

| 环节 | 现状 | 位置 |
|---|---|---|
| LIO 表面降采样 | `filter_size_surf: 1.0`（上游默认 0.5） | `config/mid360s.yaml:24` |
| 发布的 `/cloud_registered` | `dense_publish_en: false` → 发布的就是上面那个 **1.0 m 体素云** | `src/laserMapping.cpp:493`；`mid360s.yaml:58` |
| ICP 输入 scan 再降采样 | `scan_voxel_size: 0.5`，粗配再乘 5（2.5 m） | `global_localization.py:167-178`；`mid360s.yaml:4` |
| ICP 目标地图 | `map_voxel_size: 0.5` | `mid360s.yaml:3` |

三道降采样叠加后，ICP 实际可用的几何分辨率就在 0.5 m 量级。**这是当前精度最大的杠杆**，也和你自己写在 `mid360s.yaml:24` 的注释（"精度不够可降到 0.5"）一致。

**建议**：先只把"定位用的那路点云"恢复到 0.2~0.5（单独给 localization 配置开 `dense_publish_en: true`，或把 `filter_size_surf` 降到 0.5），观察 CPU 与 ICP fitness；地图体素可同步降到 0.25。注意 `filter_size_surf` 变小会让单帧点数上升，与 P2-5 的数组上界问题联动。

### P1-2 验收判据过松、FOV 裁剪退化成整图、无异常剔除

- `localization_th: 0.5`（原 0.9）：`fitness` 是"源点中在 `max_correspondence_distance` 内有对应的比例"，精配阈值 1.0 m、还有粗配 5.0 m（`global_localization.py:246`），fitness 极易 > 0.5 → **错误位姿也会被接受并发布**（`:181-192`）。
- 全向分支的裁剪掩码只判 `xs < fov_far`（`:205-207`），**没有 `xs > 0` 或半径下界**，配合 `fov_far: 100`（`mid360s.yaml:8`）→ "子图"实际≈整张地图，既费 CPU 又让 ICP 容易被远处结构拉走。
- 失败分支只保留旧位姿，**没有任何健康状态/超时提示**（`:134-136`），失败是静默的。
- 注释与默认值不符：`mid360s.yaml:5` 写"原来 1.0"，但脚本里 `freq_localization` 默认是 0.5（`global_localization.py:42`）；这类注释建议核对后再信任。

**建议**：`①` 子图改为按当前位姿的**半径裁剪**（40~60 m）；`②` 判据改成 `fitness + inlier_rmse + 重叠率` 联合，门限回到 0.8~0.9，并要求**连续 N 次一致**才更新；`③` 增加**位姿增量门限**（单次修正 > 0.3 m / 5° 就拒绝或分步收敛）；`④` 发布定位健康状态（诊断话题或 bool），失败超时后让上层降级。

### P1-3 修正量阶跃注入 + 重定位语义错误 + 无丢失检测

- `T_map_to_odom` 是 1 Hz 直接整体替换，`transform_fusion` 50 Hz 直接广播（`transform_fusion.py:145, 152-164`）→ map→odom **阶跃跳变**，Nav2 会看到位置突变。
- `/initialpose` 被当成 `map→odom` 使用（`global_localization.py:123-125, 199`）：RViz 给的是 `map→body`，**只有冷启动（odom≈I）时自洽**；车动过之后再"给初始位姿"其实算错。
- 没有"定位丢失"状态，机器人被搬动后不会自恢复。

**建议**：对修正量做限幅/低通（或用互补滤波按固定时间常数收敛）；更"正统"的是把 `/localization` 作为 `pose0` 喂进 EKF（或加第二个 `world_frame: map` 的 r_l 实例），让地图修正走协方差融合而非硬贴；把 `/initialpose` 的语义改成"map→body 初值，据此反算 `map→odom`"；补丢失检测（连续 N 次 ICP 失败 → 发诊断 + 可选让 Nav2 挂起）。

### P1-4 时戳不一致

- TF 用 `now()` 打时间戳（`transform_fusion.py:154`），`/localization` 用雷达 stamp（`:171`）。
- TF 查询用 `rclpy.time.Time()`＝最新（`:133-134`），而 EKF 是 30 Hz → 与 50 Hz 合成错配（1 m/s × 33 ms ≈ 3 cm；1 rad/s × 33 ms ≈ 1.9°）。
- 建议：查询按目标 stamp 对齐（或至少固定用最新并给 TF 打 `now()` 之外的一致时间基准），并在 `/localization` 里带上报的采样时刻。

### P1-5 地图加载链路脆弱

| 问题 | 位置 |
|---|---|
| 路径硬编码，且与建图输出的路径不是一回事 | `mid360s.yaml:16` vs `mid360.yaml:10` |
| 读 PCD 失败只打日志不退出（Open3D `read_point_cloud` 失败常返回空云而不抛异常） | `global_map_publisher.py:22-28` |
| 发布时每 5 s 用 `points.tolist()` + `create_cloud_xyz32` 全量重建 Python 列表（百万点级＝秒级卡顿 + GB 级内存抖动） | `global_map_publisher.py:39-54` |
| 收到一次地图就 `destroy_subscription` → 无法重载，map 发布者重启也接不回来 | `global_localization.py:113` |
| 地图体素 0.5 m + `estimate_normals(radius=1.5)`，法向量在大图上一次性计算会阻塞整个节点数秒 | `global_localization.py:104-110` |

**建议**：`global_map_publisher` 改用 `array.array('B', np.ascontiguousarray(points, dtype=np.float32).tobytes())`（**你们在 `global_localization.py:218-232` 已经这么做了，直接复用**），QoS 改 `TRANSIENT_LOCAL` + 只发一次；map 路径改为参数 + 启动校验；保留重订阅能力（或把地图改成服务/一次性 latched 话题）。

### P1-6 EKF 观测配置

| 问题 | 位置 | 说明 |
|---|---|---|
| `odom0_config` 同时取绝对位姿 x/y/yaw **和** vx/vyaw | `config/ekf_config.yaml:27-31` | 同源相关测量双重计数（教科书上应二选一，或 `differential: true`） |
| `transform_timeout: 0.0` | `:12` | 对 IMU 变换零等待，启动期会丢测量，建议 0.05 |
| 注释称 `process_noise_covariance` / `initial_estimate_covariance` 是"旧车调好的值"，实际与 robot_localization 默认值一致 | `:67-99` | 注释不可信，容易误导后续调参 |
| `imu0_remove_gravitational_acceleration: true` 但 IMU 不提供 orientation（`orientation_covariance[0] = -1`） | `:52-63`；`livox_ros_driver2/src/lddc.cpp` | r_l 只能用状态估计去重力；建议实测确认观测量合理 |
| `/odom` 消息协方差是否已补 | `kybot_base/.../base_controller.py` | `启动.md:38` 称"已修"，但本副本源码里未看到填协方差，需在机器人 install 目录确认 |

### P1-7 重定位/搬运恢复能力弱

只支持"人工给初始位姿 + 单初值 ICP"（`global_localization.py:115-136`）。搬动、定位丢失后无自动恢复。可选增强：Scan Context / 直方图全局检索，或"多初值并行 ICP + 评分择优"（成本：算力与内存）。

### P1-8 包外但影响定位：Nav2 bringup 可能默认启动 AMCL

`nav2_params/param_top_diff_bs.yaml` 中**没有 `amcl` 段**（全文无匹配），而 `启动.md:47` 用的是 `nav2_bringup bringup_launch.py`（默认 `slam:=False` → 会启动 `localization_launch.py`，内含 amcl + map_server）。AMCL 用默认参数（`scan_topic: scan`、`base_frame_id: base_footprint`），大概率收不到 scan 而不发布 `map→odom`；但**一旦话题名撞上，就会和 `transform_fusion` 抢 `map→odom`**。
**建议**：显式禁用 amcl（或不启动 localization_launch），或把 amcl 参数化并确认其不发布 TF。【待实测：确认实际启动的节点列表】

### P1-9 退化场景无检测

长廊/空旷/隧道只有 1 Hz ICP 纠正，没有退化检测。建议基于 ICP 的法向量分布/特征值或 `inlier_rmse` 判定退化，退化时**暂停更新**（避免把错误位姿当修正）。

---

## 4. P2 —— 性能与工程化

| 编号 | 问题 | 位置 | 建议 |
|---|---|---|---|
| P2-1 | `cube_side_length: 1000.0`（上游默认 200）→ 局部地图几乎不裁剪，ikd-Tree 长期增长；`InitializeKDTree` 从未调用，实际用的是构造函数默认 `balance=0.6` | `mid360s.yaml:26`；`laserMapping.cpp:123, 1006`；`ikd_Tree.h:309`；`ikd_Tree.cpp:10-18` | 改 200~300；显式 `InitializeKDTree(0.5, 0.7, filter_size_map)` |
| P2-2 | `MP_EN` 只在 x86 分支开启 → **Jetson（aarch64）上残差/H 构建是单线程** | `CMakeLists.txt:23-41`；`laserMapping.cpp:686-689` | 给 ARM 也开 `-DMP_EN -DMP_PROC_NUM=<核数-1>`，实测耗时 |
| P2-3 | `global_localization` 单线程 executor 里阻塞式跑两级 ICP（各 20 次迭代）+ 地图加载时 `voxel_down_sample`/`estimate_normals` → 期间所有回调排队丢帧 | `global_localization.py:100-113, 155-178` | 用 `ReentrantCallbackGroup`/`MultiThreadedExecutor`；**同时必须改掉"单线程所以存引用即可"的假设**（`:151-153`），否则变竞态 |
| P2-4 | `sync_packages()` 在**无锁**状态下读/弹出 `lidar_buffer`/`time_buffer`/`imu_buffer`，而回调在锁内修改（上游遗留的经典竞态）；冷启动时 `meas.imu` 可能为空而 `IMU_init` 直接取 `meas.imu.front()` | `laserMapping.cpp:383-435`（无锁）、`:285-305, 313-346`（锁内）、`IMU_Processing.hpp:156-193` | 给 `sync_packages` 加同一把锁并缩短临界区；对空 `meas.imu` 加保护 |
| P2-5 | 定长数组 `point_selected_surf[100000]` / `res_last[100000]` 按 `feats_down_size` 索引，而降采样参数是可变的 → 参数调细后**潜在越界** | `laserMapping.cpp:97, 443-447, 711, 730-739` | 改成 `std::vector` + `resize`，或统一上限常量并加断言 |
| P2-6 | `/global_map` 用 reliable + depth 1，每 5 s 全量重发；`/cloud_registered` 用默认 reliable depth 20，而下游订阅 depth 1 | `global_map_publisher.py:30-31`；`laserMapping.cpp:931`；`global_localization.py:70` | 大云改 `TRANSIENT_LOCAL` 只发一次；点云/里程计可考虑 `SensorDataQoS` |
| P2-7 | `publish_initial_pose.py` 立即发布后退出 | 见 P0-5 | 重复发/延时发 |
| P2-8 | `CMakeLists.txt:10` 用 `set(CMAKE_CXX_FLAGS ...)` **覆盖**外部编译标志（丢掉 `-march=native` 等）；重复 `-std=c++17`；`find_package(PythonLibs REQUIRED)` 实际未用；`package.xml` 缺 `tf2_ros`/`std_srvs`/`robot_localization`/`pointcloud_to_laserscan`/`sensor_msgs_py`/`open3d`/`tf_transformations` 等运行时依赖 | `CMakeLists.txt:8-18, 43-48`；`package.xml` | 改用 `target_compile_options`；补齐 `<exec_depend>`，否则换机器 rosdep 装不全 |
| P2-9 | `transform_fusion.py` **全文件没有 `declare_parameter`**，`FREQ_PUB_LOCALIZATION = 50.0` 硬编码 → `mid360s.yaml:10-12` 的 `freq_pub_localization` 完全不生效 | `transform_fusion.py:20`；`mid360s.yaml:10-12` | 加 `declare_parameter` 并读配置 |
| P2-10 | 零测试、零评估：无 `test/`、无 lint/launch_test、`.github` 只有 `stale.yml`；无 ATE/RPE 评估脚本；`runtime_pos_log_enable` 默认 false，日志写在包源码目录 `Log/`（只读安装目录会失败） | 全包；`common_lib.h` 的 `DEBUG_FILE_DIR` | 补冒烟测试（§7）与 bag 回放评估；日志改到 `~/.ros` 或参数化 |
| P2-11 | 脚本内节点名与 launch 不一致（`global_localization.py:37` 是 `fast_lio_localization`、`global_map_publisher.py:14` 是 `map_publisher`），被 launch 覆盖成别的名字 → `ros2 run` 单独启动时 yaml 参数不生效 | 同左 | 统一命名 |
| P2-12 | 文档与事实不符：README 提 `map→camera_init`/`map→body`（无发布者）；`/cur_scan_in_map` 实为 `odom` 系（`global_localization.py:142`）却标 map；LICENSE/package.xml 是 BSD 而 README 写 MIT；`mid360s.yaml:40` 注释"盲区 0.1 m"与实际 `blind: 0.5` 不符 | README、各 yaml | 一并订正，避免后来人踩坑 |

---

## 5. 关于"历史优化方法"的复盘

### 做得好的

1. **问题定位准**：从"转圈时定位漂"这一现象出发，沿链路找出 ICP 频率/门槛、gyro bias 收敛速度、IMU 与雷达时间同步三个不同根因，分别动对应参数，而不是乱调。yaml 里保留"原来 X"注释，可回溯。
2. **性能优化有据**：OpenBLAS 自旋（`transform_fusion.py:52-106` + `launch:74` 的 `OPENBLAS_NUM_THREADS=1`）、日志 2 s 节流、PointCloud2 向量化解析（`global_localization.py:82-98`）、可视化话题按订阅者跳过（`:143-145, 218-220`）、`array.array('B')` 直通、scan 与 odom 原子配对、单线程 executor 免锁免拷贝——都是"少做无用功"，且注释解释了为什么。
3. **架构上理清 TF 所有权**：EKF 独占 `odom→base_link`，底盘 `publish_tf: false`（`arduino.yaml`、`base_controller.py`），避免双发布者；`base_frame` 统一为 `base_link`。
4. **倾斜补偿放在驱动层**（`livox_ros_driver2`）：因为 FAST-LIO 初始化时 `init_state.rot = I`、重力用初始 body 系表示（`IMU_Processing.hpp:193`，`:195` 的自动对齐被注释），不做补偿地图 z 轴就是斜的、与 `pcd2pgm` 的 2D 地图对不上。放在驱动层一次解决所有下游，是这轮里最聪明的一手。
5. **IMU 协方差按 BMI088 量级补齐**、`orientation_covariance[0] = -1`（`lddc.cpp`）：原来全 0 会被 r_l 当"无限精确"。

### 不足的

1. **缺量化验证闭环**：没有 bag 回放、没有 ATE/RPE、ICP fitness/rmse 不落盘。"门槛 0.9→0.5""体素 0.1→0.5"这类改动无法证明是净收益而非"用精度换稳态"。
2. **跨模块隐式耦合**：28.38° 只写在驱动 launch 里，本包 launch 又写"旋转填 0，不要再填底座倾角"——两个互相依赖的参数分居两个包，改一个忘一个就静默错位（P0-8 就是它的直接后果）。
3. **关键假设没落到断言上**："LIO body ≈ base_link""LIO odom ≈ EKF odom"是整个链路的地基，代码里既无检查也无注释（P0-1/P0-2 由此而来）。建议加一个启动自检节点：比较 `/Odometry` 与 TF `odom→base_link` 的差、比较 `/localization` 与 `map→base_link`，超阈值就告警。
4. **优化过程没进 git**：初始提交把上游第三方代码和本地改造混在一起，没做 submodule/subtree/分支，导致无法 diff 上游、无法合并上游更新（见 §1.1）。

### 一个提醒

如果之前判断"转圈漂不漂"是靠肉眼看 `/localization` 或 RViz 里的机器人模型，那么**观察结果里混了 P0-1 的伪影**（原地转圈上报位置自己画半径 0.4 m 的圆）。修完 P0-1 后值得重跑一次同样的测试，才能看到 LIO/ICP 的真实水平。

---

## 6. 建议的修复顺序

### 第 1 批：不改硬件、不重编译（半天，风险最低，收益最直接）

1. **P0-2**：按 §9 落地最小改动（新增 `scripts/cloud_to_base_link.py` + launch 2 处 + `CMakeLists.txt` 1 行）。只装脚本、不编译 C++；旁路改动，可一键回滚。
2. **P0-1 方案 A**：`transform_fusion.py` 插入杆臂修正（3 行）+ `/localization` 的 `child_frame_id` 改名。建议在 P0-2 验证通过后再做——两次分开改，出问题好定位。
3. **P0-3**：`use_sim_time` 默认值改 `false`，全部节点统一注入。
4. **P0-7**：RViz 配置名拼写。
5. **P0-5**：`laserMapping.cpp` 协方差填充移到 `publish()` 之前（**这条要重编译，可挪到第 3 批**，但改动只有一行）。
6. **P2-9**：`transform_fusion` 加 `declare_parameter`。
7. **§1.2 / §1.3**：`git add -A` 重建索引并提交；`S2.hpp` 编码修正单独提交。

### 第 2 批：配置与脚本（1 天）

- P0-2 的另外两个备选（LIO 世界系改名并接进 TF / 换二维雷达）——若 §9 方案验证通过则不必做
- P0-4（盲区过滤加括号，需重编译，可与第 3 批合并）
- P0-6（map 路径参数化 + 启动校验；`/map_save` 语义订正进 `启动.md`）
- P1-2（半径裁剪 + 联合判据 + 增量门限）
- P1-5（`global_map_publisher` 用 tobytes + `TRANSIENT_LOCAL`）
- P1-6（EKF 观测改为 twist 或 differential；`transform_timeout` 0.05）
- P1-4（时戳对齐）

### 第 3 批：需要重编译或结构调整

- P0-8（外参按 `R_m·T` 重算并统一两套配置）
- P1-1（`filter_size_surf` / `map_voxel_size` / `dense_publish_en` 联动调整）
- P2-1、P2-2、P2-4、P2-5、P2-8
- P2-3（executor 改造，注意同步改单线程假设）

### 第 4 批：体系化

- §7 的冒烟测试 + bag 回放评估管线（P2-10）
- P1-3（修正量平滑 / 接进 EKF）+ P1-7（重定位增强）+ P1-9（退化检测）
- P1-8（确认并禁用 AMCL）
- 决策：继续 3D→scan、换二维雷达、还是把 `PointCloud2` 直接喂 costmap

---

## 7. 可执行的验证方法

### 7.1 冒烟测试（10 秒内应全部满足）

```bash
ros2 topic hz /Odometry              # ≈10 Hz（雷达帧率）
ros2 topic hz /localization          # ≈50 Hz
ros2 run tf2_ros tf2_echo map base_link   # 有数值，非 NaN
ros2 topic echo /odom --once         # 协方差不全为 0
ros2 run tf2_ros tf2_echo odom base_link  # EKF 正常时有数值，NaN 就是 EKF 炸了
```

### 7.2 P0-1 专项（杆臂）

```bash
# 车推到空地，原地缓慢转一整圈，同时观察：
ros2 run tf2_ros tf2_echo map base_link
# 修前：(x,y) 画半径约 0.4 m 的圆（车心未动）
# 修后：基本不动，残余 < 5 cm
```

辅助验证：在 map 里发一个目标点让车到点，卷尺量车心到目标的实际距离（修前约差 0.4 m）。

### 7.3 P0-4 专项（盲区）

车静止，看 `/cloud_registered` 中距原点最近的点是否 > `blind`（0.5 m）。修前会看到大量 < 0.5 m 的点（含车体/支架）。

### 7.4 P0-2 专项（同名异系）

```bash
# 静止时比较两个里程计的世界位姿，差值应在厘米级；跑几分钟后再比，若明显增大即为本条
ros2 topic echo /Odometry --field pose.pose.position --once
ros2 run tf2_ros tf2_echo odom base_link
```

### 7.5 回归对比（任何调参前后都要做）

用 rosbag 录一段固定路线（含原地转圈、贴墙、长走廊），用 evo 或自写脚本算 ATE/RPE，并把 ICP 的 `fitness`/`inlier_rmse`、LIO 单帧耗时一起落盘。**没有这一步，后续所有调参都只是"感觉好了"。**

---

## 8. 未验证 / 待实测项

| 项 | 为什么没定论 |
|---|---|
| robot_localization 对全 0 协方差输入的具体处理 | 需看 r_l 版本源码或实测 |
| `/odom` 协方差是否已补 | `启动.md` 说已修，但本副本 `base_controller.py` 中未见填协方差，需查机器人 install 目录 |
| `nav2_bringup` 实际是否启动了 AMCL | 需看实际节点列表 |
| 建图 launch 的工作目录（决定 `./test.pcd` 落在哪） | 取决于启动 `ros2 launch` 时的 cwd |
| `mount_pitch` 的符号与数值是否与实际安装一致 | 需实测（验证方法：车静止时看 `/cloud_registered` 中地面的法向是否≈(0,0,1)） |
| `extrinsic_T` 两套配置哪个正确、补偿后是否需按 `R_m·T` 填 | 需标定确认 |
| `mid360_handler` 是否会被走到 | 当前配置 `lidar_type: 1` + CustomMsg 走 `avia_handler`；只有改成 PointCloud2 + `lidar_type=MID360` 才会命中（内部用 `line` 直接索引 `is_first[N_SCANS]`，缺越界校验，属潜在隐患） |
| Open3D `voxel_down_sample` 是否丢法向量 | 与版本相关，不影响当前用法 |
| `MP_EN` 在 Jetson 上开启后的实际收益 | 需实测耗时 |

---

## 9. 附录：P0-2 最小改动实施方案（含完整代码，可直接落地）

**目标**：消除 `/cloud_registered` 的坐标系借用问题，使 `/scan_fe` 的障碍位置不再随两个里程计的分歧漂移。

**边界（明确不动的东西）**：不编译 C++、不改 LIO、不改 EKF、不改 `map→odom` 的算法、不改 `nav2_params`。新节点是旁路，出问题把 launch 那两行改回去即恢复原状。

### 9.1 数据流对比

```
改动前：  /cloud_registered (frame=odom，实为 W_L) ──┐
                                                    ├─→ pointcloud_to_laserscan (target_frame: base_link)
                                          TF 查 odom─┘        └─→ /scan_fe （障碍偏 T_{W_L←O}）

改动后：  /cloud_registered (不动)  ┐
                                   ├─→ cloud_to_base_link (新, 用 /Odometry 自己算)
                     /Odometry ────┘        └─→ /cloud_registered_base (frame=base_link)
                                                      └─→ pointcloud_to_laserscan (target_frame: "")  └─→ /scan_fe ✓
```

关键点：**新节点不做任何 TF 查询**，直接用 LIO 自己的 `/Odometry` 求逆把点云转到 `base_link`——不经过 `odom` 这个名字，偏差恒为 0。

### 9.2 改动 1/3：新增 `scripts/cloud_to_base_link.py`

```python
#!/usr/bin/env python3
# coding=utf-8
"""把 /cloud_registered 从 FAST-LIO 世界系(W_L) 变换到真实 base_link 后重发。

背景: /cloud_registered 的 frame_id 写作 "odom"，但它实际在 LIO 自己的世界系里；
      TF 里的 odom 是 EKF 的 odom 系，两者随里程分歧 → /scan_fe 障碍位置漂移。
      本节点不查 TF，直接用 /Odometry（LIO 自身位姿）求逆后转到 base_link，
      所以偏差恒为 0。
"""
import array
import numpy as np
import rclpy
from rclpy.node import Node
from sensor_msgs.msg import PointCloud2, PointField
from nav_msgs.msg import Odometry

# base_link <- body(≈livox_frame/IMU) 的平移: 用 launch 里静态 TF (0.40,0,0.22) 取负。
# 残差 ≈ 雷达内部 LiDAR↔IMU 的 ~5cm，可忽略；要精确就用实标外参。
T_BASE_FROM_BODY = np.eye(4)
T_BASE_FROM_BODY[:3, 3] = (-0.40, 0.0, -0.22)


def quat_to_mat(x, y, z, w, tx, ty, tz):
    """四元数+平移 → 4x4（逐元素运算，不触发 BLAS，与 transform_fusion 风格一致）"""
    n = x * x + y * y + z * z + w * w
    s = 2.0 / n if n > 0.0 else 0.0
    xx, yy, zz = x * x * s, y * y * s, z * z * s
    xy, xz, yz = x * y * s, x * z * s, y * z * s
    wx, wy, wz = w * x * s, w * y * s, w * z * s
    return np.array([[1.0 - (yy + zz), xy - wz, xz + wy, tx],
                     [xy + wz, 1.0 - (xx + zz), yz - wx, ty],
                     [xz - wy, yz + wx, 1.0 - (xx + yy), tz],
                     [0.0, 0.0, 0.0, 1.0]])


class CloudToBaseLink(Node):
    def __init__(self):
        super().__init__('cloud_to_base_link')
        self.last_odom = None
        self.pub = self.create_publisher(PointCloud2, '/cloud_registered_base', 1)
        self.create_subscription(Odometry, '/Odometry', self.on_odom, 1)
        self.create_subscription(PointCloud2, '/cloud_registered', self.on_cloud, 1)
        self.get_logger().info('cloud_to_base_link ready')

    def on_odom(self, msg):
        # laserMapping 在同一个回调里先发 /Odometry 再发点云，且两者 stamp 相同
        self.last_odom = msg

    def on_cloud(self, msg):
        if self.last_odom is None:
            return
        offs = {f.name: f.offset for f in msg.fields}
        if not {'x', 'y', 'z'} <= offs.keys():
            return

        o = self.last_odom.pose.pose
        T_wl_body = quat_to_mat(o.orientation.x, o.orientation.y, o.orientation.z,
                                o.orientation.w, o.position.x, o.position.y, o.position.z)
        R, t = T_wl_body[:3, :3], T_wl_body[:3, 3]
        # T(base<-W_L) = T(base<-body) @ inv(T(W_L<-body))
        R_T = T_BASE_FROM_BODY[:3, :3] @ R.T
        t_T = T_BASE_FROM_BODY[:3, 3] - R_T @ t

        dtype = np.dtype({'names': ['x', 'y', 'z'],
                          'formats': [np.float32] * 3,
                          'offsets': [offs['x'], offs['y'], offs['z']],
                          'itemsize': msg.point_step})
        buf = array.array('B', msg.data)          # 一次 C 级拷贝
        xyz = np.frombuffer(buf, dtype=dtype)     # array.array 可写 → 原地改 x/y/z
        if xyz.size == 0:
            return
        p = np.stack([xyz['x'], xyz['y'], xyz['z']], axis=-1).astype(np.float64)
        p = np.einsum('ij,kj->ki', R_T, p) + t_T  # 等价 p @ R_T.T + t_T，不触发 BLAS
        xyz['x'], xyz['y'], xyz['z'] = p[:, 0], p[:, 1], p[:, 2]

        out = PointCloud2()
        out.header.stamp = msg.header.stamp
        out.header.frame_id = 'base_link'         # ← 关键：不再叫 odom
        out.height, out.width = msg.height, msg.width
        out.fields, out.is_bigendian = msg.fields, msg.is_bigendian
        out.point_step, out.row_step, out.is_dense = msg.point_step, msg.row_step, msg.is_dense
        out.data = buf
        self.pub.publish(out)


def main(args=None):
    rclpy.init(args=args)
    node = CloudToBaseLink()
    try:
        rclpy.spin(node)
    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == '__main__':
    main()
```

### 9.3 改动 2/3：`launch/velodyne_localization.launch.py`

```python
# ① 新增节点
cloud_to_base_link_node = Node(
    package='fast_lio_localization',
    executable='cloud_to_base_link.py',
    name='cloud_to_base_link',
    output='screen'
)

# ② pointcloud_to_laserscan：换输入话题 + target_frame 置空
pointcloud_to_scan_node = Node(
    package='pointcloud_to_laserscan',
    executable='pointcloud_to_laserscan_node',
    name='pointcloud_to_laserscan',
    remappings=[('cloud_in', '/cloud_registered_base'),   # ← 原为 /cloud_registered
                ('scan', '/scan_fe')],
    parameters=[{
        'target_frame': '',          # ← 空 = 不做任何 TF 变换（点云已在 base_link）
        'transform_tolerance': 0.05, # 已无 TF 查询，留着无害
        'min_height': -0.12,         # 仍在 base_link 系下切片，语义不变
        'max_height': 1.2,
        'angle_min': -3.14159, 'angle_max': 3.14159, 'angle_increment': 0.00436,
        'scan_time': 0.1, 'range_min': 0.6, 'range_max': 4.5,
        'use_inf': True, 'inf_epsilon': 1.0,
    }],
    output='screen'
)

# ③ 在 ld.add_action(pointcloud_to_scan_node) 之前加一行
ld.add_action(cloud_to_base_link_node)
```

**顺带白捡两个好处**：`target_frame: ''` 后 p2l 完全不查 TF → 启动期/时间戳抖动导致的"scan 时有时无"风险一并消失；`min/max_height` 的语义与注释（"base_link 系下的高度"）从此真正成立。

### 9.4 改动 3/3：`CMakeLists.txt`

```cmake
install(PROGRAMS
  scripts/transform_fusion.py
  scripts/global_localization.py
  scripts/publish_initial_pose.py
  scripts/global_map_publisher.py
  scripts/cloud_to_base_link.py      # ← 新增
  DESTINATION lib/${PROJECT_NAME}
)
```

部署：`colcon build --packages-select fast_lio_localization --symlink-install`（**只装脚本，不编译 C++，几秒钟**）。

### 9.5 为什么这是"最小"

| 别的思路 | 为什么更大 |
|---|---|
| 改 `src/laserMapping.cpp:508` 把 `"odom"` 改成 `"camera_init"` | 光改名不行：`camera_init` 不在 TF 树里 → p2l 直接查不到变换、scan 发不出来，必须同时补 TF 边 |
| 给 `W_L` 起名并接进 TF 树 | 要新增 TF 边、处理时间戳/插值，还改变 TF 拓扑（以后别人再补 `map→camera_init` 会撞车） |
| 动 `nav2_params` | 改不了任何东西——costmap 配置本身没错（`/scan_fe` 配得对） |
| 动 LIO / EKF 参数 | 与误差无关，纯白调 |

### 9.6 改完什么变好、什么还在

- ✅ **P0-2 消失**：障碍位置不再随里程漂移；局部 costmap（`odom` 系）的机器人与障碍**双双正确**；全局 costmap 里障碍也落在正确的 map 坐标上。
- ⚠️ **P0-1 的 0.4 m 仍在**：那是机器人自身在 `map` 里挂错点，属于 `transform_fusion.py:145` 那 3 行（同样不重编译）。建议 P0-2 验证通过后再改，两次分开便于定位。
- ℹ️ RViz 里原先显示 `/cloud_registered` 的，可换成 `/cloud_registered_base`（这才是老实标了 `base_link` 的那一路）。

### 9.7 验证

```bash
# 1) 话题链
ros2 node info /pointcloud_to_laserscan         # Subscribers 应为 /cloud_registered_base
ros2 topic hz /scan_fe                          # 应 ≈10 Hz 稳定
ros2 topic echo /scan_fe --field header --once  # frame_id 应为 base_link

# 2) 关键验证：走一个闭环回到出发点
#    改之前：把 /scan_fe 或点云叠到静态地图上，墙会随里程错开
#    改之后：跑多久墙都与静态地图重合（变换由 LIO 自身位姿给出）

# 3) 回归：确认定位没受影响
ros2 topic hz /localization                     # 仍 ≈50 Hz
ros2 run tf2_ros tf2_echo map base_link         # 有值、非 NaN
```

### 9.8 回滚

launch 里把 `cloud_in` 改回 `/cloud_registered`、`target_frame` 改回 `base_link` 即可；新节点留着不发也没人订阅，零副作用。

---

## 附录 A：关键证据行号索引

| 主题 | 位置 |
|---|---|
| IMU body 位姿冒充 base_link | `src/laserMapping.cpp:615-626, 631-632` |
| LIO 的 TF 广播被注释 | `src/laserMapping.cpp:648-659` |
| `/cloud_registered` 标 `odom` | `src/laserMapping.cpp:508` |
| 杆臂换算处 | `scripts/transform_fusion.py:133-135, 145, 173` |
| 静态 TF (0.40, 0, 0.22) | `launch/velodyne_localization.launch.py:103-108` |
| EKF 独占 odom→base_link | `config/ekf_config.yaml:15, 21`；`kybot_base/ros_arduino_python/config/arduino.yaml` |
| `use_sim_time` 默认 true / 未统一注入 | `launch/velodyne_localization.launch.py:24-27, 103-142` |
| 盲区过滤优先级 bug（avia 路径） | `src/preprocess.cpp:47-51, 95-194（183-186）` |
| 协方差滞后一帧 | `src/laserMapping.cpp:629-646` |
| `/map_save` 依赖 `pcd_save_en` + `map_en` | `src/laserMapping.cpp:840, 1111-1130` |
| 驱动倾斜补偿 | `livox_ros_driver2/src/lddc.cpp`；`livox_ros_driver2/launch_ROS2/msg_MID360s_launch.py:32-36` |
| 重力用初始 body 系表示 | `src/IMU_Processing.hpp:193-195` |
| `b_gyr_cov` 确实生效（非 bug） | `src/laserMapping.cpp:899-902`；`src/IMU_Processing.hpp:91-92, 146-148` |
| ICP 两级与判据 | `scripts/global_localization.py:167-178, 181-192, 197-216, 238-251` |
| 地图发布缺陷 | `scripts/global_map_publisher.py:22-28, 30-31, 39-54` |
| Nav2 局部/全局代价地图 | `nav2_params/param_top_diff_bs.yaml:65, 73, 90, 115, 128` |
| 已知问题（自述） | `启动.md:3`（三维转 scan 不可行） |

## 附录 B：审计范围与局限

- 范围为**静态审计**：逐行阅读 `src/laserMapping.cpp`(1199 行)、`src/preprocess.cpp`(1069 行)、`src/IMU_Processing.hpp`、`include/`、`include/ikd-Tree/`、`scripts/*.py`、`launch/*.py`、`config/*.yaml`、`CMakeLists.txt`、`package.xml`，以及包外 `livox_ros_driver2`、`kybot_base`、`nav2_params`、`启动.md`。
- **未在机器人上运行**；所有"影响"均为基于代码逻辑的推导，建议按 §7 逐条实测确认后再改动。
- 上游为 `myeongw002/FAST_LIO_LOCALIZATION_ROS2`（见 `README.md:55`）；本次无法联网逐文件 diff 上游，因此"哪些是上游遗留、哪些是本地新增"依据代码注释（中文注释＝本地改动）与 git 历史判断，已在文中分别标注。
